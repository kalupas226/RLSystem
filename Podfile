# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RLSystem' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RLSystem
  pod 'RealmSwift'
  pod 'CSV.swift'
  pod 'R.swift'
  pod 'AlamofireImage', '~> 3.5'
  pod 'GoogleMaps'

  target 'RLSystemTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RLSystemUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
